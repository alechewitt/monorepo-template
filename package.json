{
  "name": "react-nodejs-app",
  "version": "1.0.0",
  "description": "React frontend with Node.js Express backend",
  "main": "server/index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon server/index.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^7.6.0"
  },
  "keywords": ["react", "nodejs", "express"],
  "author": "",
  "license": "MIT"
}